# ============================================================================
# 白名单式 .gitignore —— 默认忽略一切，只放行下面明确列出的东西
# ============================================================================
# 为什么不用"黑名单"（列一堆要忽略的目录）：
#   这个项目目录里**同时**存在过下面这些东西，黑名单漏一条就是**永久泄露**
#   （git 历史删不干净，只能改写历史 + 作废密钥）：
#     · migration\  —— 含真实 API 凭据（.credentials.yaml，3 份）
#                      与全部会话日志（几十个 session.jsonl.zstd，最大 17 MB）
#     · runtime\    —— 含带 token 的内核地址日志
#     · release\    —— 每个 exe 90+ MB
#   反过来，白名单漏一条只是"少传一个文件"，代价小得多。
#   ⇒ 加新东西进仓库时，**必须显式写进放行清单**，这一步就是安全闸门。
# ============================================================================

/*
!/.gitignore
!/.gitattributes
!/LICENSE
!/README.md
!/package.json
!/启动DSH.cmd
!/架构设计-v0.1-草案.md
!/src/
!/scripts/
!/assets/
!/docs/
!/tools/
!/release-notes/
!/plugin/

# ── 目录放行之后，再排掉目录里具体不该公开的文件 ────────────────────────
# 这几份是**本机专属**的迁移记录，含 C:\Users\<用户名>\... 字面路径
# （对人没用、对隐私有害；而且路径在别人机器上根本不成立）
/docs/2026-09-20-环境迁移与验证记录.md

# ★ `github/` 是**分发暂存区**（由 scripts/make-release-bundle.js 生成），
#   里面是 90 MB 级的安装包 ⇒ **故意不进 git**（它本来就是要上传到 Release 的东西）。
#   它不在上面的放行清单里，所以自动被 `/*` 忽略；这里写出来只是为了让意图可见。

# ── 兜底：任何位置都不许出现这些（即使上面放行了目录）──────────────────
**/node_modules/
**/migration/
**/runtime/
**/release/
**/.npmcache/
**/.git/
**/*.exe
**/*.msi
**/*.log
**/*.bak-*
**/.credentials.yaml
**/settings.yaml
**/cordis.patch.yml
**/session.jsonl.zstd
**/session.v3.jsonl.zstd
**/.env
**/*.token
**/*.pfx
**/kernel.json
**/smoke-result.json
**/window-state.json

# ── 兜底的**唯一例外**：客户端插件必须自带它的 cordis.patch.yml ──────────
# 上面 `**/cordis.patch.yml` 那条兜底是为了不把 DSH home / profile 里的配置提交进来；
# 但插件包里的那一份是**bundle 声明的一部分** —— 没有它内核组合 profile 时直接失败：
#     Error: dsh: profile bundle "…" declares no dsh.bundle in its package.json
# （2026-09-21 由 scripts/plugin-check.js 第一次真跑抓到）。
# 漏掉它的后果不是"少个文件"，而是**提交出去的是坏的插件**。
# git 的规则：父目录被放行了，这里才能再把它捞回来 —— 所以 `!/plugin/` 必须先存在。
!/plugin/**/cordis.patch.yml

# ── 插件的 Android 工程：只收源码，**不收 Gradle 构建产物** ──────────────
# `plugin/dsh-mobile-remote/android/` 里的 .gradle 缓存与 build/ 中间件约 4.1 MB，
# 而且有 **232 字符深**的路径（超过 Windows 260 的经典上限，cpSync 真会失败）。
# 这些都是**可再生的构建产物**，对人没有价值。
# ★ 打包侧用 extraResources 的 filter 排掉的是同样三个目录 —— 两处必须一致，
#   否则会出现"仓库里有、exe 里没有"（或反过来）的鬼打墙。
**/android/.gradle/
**/android/build/
**/android/app/build/
