# DSH 集成桌面端 —— 架构设计（v0.1 草案）

> **项目**：`D:\deepseek-workspace\5.DSH集成桌面端`
> **目标仓库**：`18477514055/DSH-Integrated-Desktop`（做完再传）
> **平台**：暂时只做 Windows
> **终极目标**：**再也不用打开社区客户端**，日常直接用本客户端
> **铁律**：封装层**绝不与内核冲突** + 外壳能**追上内壳版本**

---

## 〇、先说清楚：什么叫"绝不与内核冲突"

这句话听起来像原则，其实要落到**四个具体战场**上。
以下四条都是**实测核实**的（2026-09-19）：

| # | 冲突面 | 现状 | 风险 |
|---|---|---|---|
| 1 | **端口** | 都默认 `3080` | 两个客户端同时启动会抢端口 |
| 2 | **数据目录** | 都读 `~/.dsh` | ⚠️ **升级时会互相重写** |
| 3 | **插件镜像** | `~/.dsh/profiles/node_modules`（241 个包） | ⚠️ **跨世代会被整体重写** |
| 4 | **内核版本** | 社区版嵌 `0.1.2-rc.1`，全局 CLI 是 `0.1.5-rc.2` | ⚠️ **不同世代混用 = 插件全废** |

> **冲突面 2、3、4 是同一个病根**：多个内核实例**共享同一个 `~/.dsh`**，
> 而 profile 的模块镜像**必须与宿主内核同世代**。
> 上次事故的本质就是这个 —— 不是"升级写错了"，是**架构允许两个内核抢同一份镜像**。

### 设计对策（三条，缺一不可）

**对策 A：外壳不拥有内核，只调用内核**

```
❌ 社区版：外壳 + 内嵌内核（内核在安装目录里）  ⇒ 升级 = 拆自己地基
✅ 本项目：外壳 + 调用外部内核（内核由 npm 独立管理）⇒ 升级 = 换个引用
```

**对策 B：一个 DSH_HOME，只允许一个内核实例写**

外壳启动前**先探测**，已有内核就**复用**（attach），绝不自建第二个。

**对策 C：内核版本由"单一事实来源"决定，外壳只读不猜**

外壳**不写死版本号**，而是去问：
- 内核装在哪 → 读它的 `package.json`
- 有没有新版 → 查 npm registry
- 要不要更新 → **交给用户决定**（不自动升级，见 §四）

---

## 一、整体架构

```
┌─────────────────────────────────────────────────────────┐
│  ① 外壳层  shell/            ← 本项目的主战场            │
│     · 窗口 / 托盘 / 原生体验                              │
│     · 内核进程生命周期管理（启动/复用/停止）              │
│     · 版本检查（追内核）                                  │
│     · 崩溃自检                                            │
│     ⛔ 不含任何内核代码，不写任何内核文件                 │
└───────────────────────┬─────────────────────────────────┘
                        │ spawn（子进程边界 = 唯一接口）
                        ▼
┌─────────────────────────────────────────────────────────┐
│  ② 内核层  外部 @deepseek-ai/dsh@<版本>                  │
│     由 npm 独立管理（全局或独立目录）                     │
│     ⛔ 外壳不改它、不嵌它、不替它升级                     │
└───────────────────────┬─────────────────────────────────┘
                        │ HTTP 127.0.0.1:<port>
                        ▼
┌─────────────────────────────────────────────────────────┐
│  ③ 界面层  官方 web UI（内核自带）                       │
│     本轮先一比一使用，后续做界面优化（见 §三）            │
└─────────────────────────────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────┐
│  ④ 数据层  ~/.dsh（会话 / 插件 / 配置）                  │
│     ⚠️ 与社区版共享 ⇒ 必须遵守"单实例写"规则              │
└─────────────────────────────────────────────────────────┘
```

**关键设计**：外壳与内核之间**只有一条通道 —— 进程边界**。
外壳通过 `spawn` 起内核、读它的 stdout、加载它的网页。
内核内部怎么改，对外壳都是黑盒 ⇒ **这是"不冲突"的结构性保证**。

---

## 二、第一个任务：封装层骨架

**目标**：能起内核 + 能查版本 + 能自检，**先不加界面功能**。

### 2.1 内核发现策略（优先级从高到低）

```
① 用户显式指定（配置里的 kernel.path）
② 环境变量 DSH_KERNEL_PATH
③ 全局 npm 安装   %APPDATA%\npm\node_modules\@deepseek-ai\dsh
④ 项目本地       <项目>\vendor\dsh
⑤ npx 回退       npx -y @deepseek-ai/dsh@<探测到的版本>
```

> 已核实：你的机器上 **③ 已存在**（`0.1.5-rc.2`，190 个依赖完整，
> 实测能带你的真实插件启动，stderr 零报错）。

### 2.2 启动流程（防冲突版）

```
1. 读配置，确定 DSH_HOME / 端口 / profile
2. 探测目标端口
     已有 dsh 在跑？
       ├─ 是 → attach 模式：直接加载它的 URL（**绝不自建第二个**）
       └─ 否 → 继续
3. 检查内核是否存在且完整（bin.js + 依赖）
4. spawn 内核（--host --port --no-open）
5. 从 stdout 解析 `dsh web: http://...` 拿 URL
6. 探活；就绪后加载页面
7. 运行期每 N 秒探活；掉线自愈
```

### 2.3 要做到的硬指标

| 指标 | 要求 |
|---|---|
| 外壳代码里出现 `@deepseek-ai/` 的次数 | **0**（除了"发现内核"那一段只读路径） |
| 外壳是否会写 `~/.dsh` | **只有配置文件，绝不碰 `profiles/`** |
| 外壳是否内嵌内核依赖 | **否**（`package.json` 里不出现 `@deepseek-ai/dsh`） |
| 已有内核在跑时 | **必须复用，不自建** |

---

## 三、后续功能（界面优化 + 客户端内新增）

> 你说了：**集中在界面优化与客户端内功能新增**，以后可能加工具。

### 3.1 界面优化（候选，待你挑）

| # | 想法 | 实现层 | 难点 |
|---|---|---|---|
| 1 | 深色/浅色主题跟随系统 | 注入 CSS 或主题插件 | 官方 UI 是打包产物，**不该直接改** |
| 2 | 分栏布局 / 多会话并排 | 外壳层（多窗口）或插件 | 看官方 UI 是否支持 |
| 3 | 快捷键增强 | 外壳层 | 容易 |
| 4 | 通知增强（完成/出错区分） | 外壳层 | 容易 |
| 5 | 窗口置顶 / 迷你模式 | 外壳层 | 容易 |
| 6 | 启动动画 / 状态页 | 外壳层 | 已有社区版参考 |

> ⚠️ **界面优化的原则**：优先在**外壳层**做（多窗口、快捷键、通知），
> 其次做成**dsh 插件**（注入 UI），**最后才考虑**改官方前端产物
> —— 因为官方前端是打包产物，改了每次升级都要重打。

### 3.2 客户端内新功能（候选，待你挑）

| # | 功能 | 说明 |
|---|---|---|
| 1 | **内核版本检查 + 更新提示** | ⭐ 本任务已含（见 §四） |
| 2 | **崩溃自检面板** | ⭐ 你要的（见 §五） |
| 3 | 插件管理器 | 图形化装卸插件（现在靠改 JSON） |
| 4 | profile 切换器 | web / clean / headless 一键切 |
| 5 | 模型通道管理 | 集成你现有的 `switch-channel-model.mjs` |
| 6 | 用量看板 | 可整合 `3.AI用量仪表盘` |

---

## 四、版本追踪：让外壳追上内壳

> 这是你点名的第一个能力。设计要点：**检查更新 ≠ 自动升级**。

### 4.1 职责边界（吸取事故教训）

| 动作 | 谁做 | 为什么 |
|---|---|---|
| **检查**有没有新版 | ✅ 外壳可自动做 | 只读、无风险 |
| **提示**有新版本 | ✅ 外壳可自动做 | 无风险 |
| **下载**新内核 | ⚠️ 需用户确认 | 会改环境 |
| **切换**到新内核 | ⛔ **必须用户手动** | 上次事故就是这里 |

> ⚠️ 这条直接对应 `AGENTS.md` 第一条：
> **会打断用户工作的不可逆操作，不由 AI 无人值守执行。**

### 4.2 版本检查逻辑

```
1. 读当前内核版本：外部内核的 package.json → version
2. 查 npm registry 最新版（带缓存，避免频繁请求）
3. 对比：
     相同        → 显示"已是最新"
     有新版      → 显示"可更新到 X"（**只提示，不自动装**）
     外壳要求更高 → 显示"内核过旧，建议更新"
4. 用户点了更新 → 走**手动流程**（见 4.3），不在后台跑
```

### 4.3 更新流程（必须可回退）

```
① 备份（内核引用 + 关键配置）
② 记录回滚点
③ 安装新内核（独立目录，不动旧的）
④ **真启动验证**（不是检查文件！加载插件，看 stderr）
   ├─ 通过 → 切换引用，提示用户
   └─ 失败 → 回滚，报告原因
⑤ 全程输出可见，用户能看到每一步
```

> ⚠️ **第 ④ 步是上次事故的教训**：当时只验证了"文件能否解析"就判定通过，
> 而真实启动时插件激活失败。**必须真启动 + stderr 为空**。

---

## 五、崩溃自检（社区版没有，我们做出来）

> 你说：社区其实没有一个很好的崩溃自检方法。

### 5.1 可以借鉴的（你的 `03-DSH崩溃自救工具箱`）

已有这些脚本，逻辑可直接搬（它们是你的资产）：

| 脚本 | 作用 |
|---|---|
| `dsh-check.ps1` | 只读自检：补丁、模块镜像、启动模式、插件、禁用项、日志 |
| `dsh-boottest.ps1` | 临时内核试启动，3–10 秒出结论，**不改配置** |
| `dsh-clean.ps1` | 纯净启动（不加载第三方插件） |
| `dsh-browser.ps1` | 官方 CLI + 浏览器模式 |
| `dsh-rollback.ps1` | 撤掉外壳补丁 |

### 5.2 本项目要做成什么

**不是脚本，是界面。** 目标：**一个页面告诉你哪里坏了、怎么修**。

```
崩溃自检面板
├─ 一键体检（绿色/黄色/红色）
│   ├─ 内核：存在？完整？版本？
│   ├─ 模块镜像：世代是否与内核一致？   ← 上次事故的核心
│   ├─ 插件：逐个能否加载？              ← 必须真启动验证
│   ├─ 端口：被谁占用？
│   ├─ 配置：语法对不对？
│   └─ 日志：最近的错误摘要
├─ 一键修复（每个问题给具体动作）
└─ 逃生通道（纯净模式 / 浏览器模式 / 回滚）
```

### 5.3 关键设计：**兜底必须覆盖正确的层**

> 上次事故的教训：故障在**官方层**，而"纯净模式"只绕过**第三方插件层**
> ⇒ **所有逃生通道同时失效**。

所以本项目的自检要能回答：**"故障在哪一层？"**

| 故障层 | 该用的逃生通道 |
|---|---|
| 第三方插件 | 纯净模式（`--profile clean`） |
| 官方 bundle | 换内核版本 / 重装内核 |
| 外壳自身 | 浏览器模式（完全绕开外壳） |
| 数据/配置 | 回滚配置 / 重建 profile |

---

## 六、技术栈（待你拍板）

我**没有替你决定**，因为这是你的项目。给三选项：

| 选项 | 体积 | 内存 | 能改的深度 | 备注 |
|---|---|---|---|---|
| **Electron** | ~150MB | 高 | 深 | 社区版用它，资料多、你熟悉 |
| **Tauri** | ~10MB | 低 | 中（Rust） | 体积优势大，但要学 Rust |
| **无外壳（快捷方式+浏览器）** | 0 | 最低 | 浅 | 最稳，但没有托盘/原生体验 |

> **我的倾向**：如果你要"再也不用打开社区客户端"且要托盘/通知/快捷键，
> **Electron 最稳**（和社区版同栈，参考成本最低，且能跑你现有的一切）。
> 但**这一条我不替你定**。

---

## 七、已定的决策（2026-09-19 用户拍板）

| # | 问题 | **决定** | 理由（用户原话） |
|---|---|---|---|
| 1 | **技术栈** | ✅ **Electron** | "electron 开头了" |
| 2 | **内核放哪** | ✅ **项目自带**（`vendor/dsh/`） | 由第 3 条"完全独立"推出 |
| 3 | **DSH_HOME** | ✅ **完全独立** | "我认为最好的就是完全独立" |
| 4 | 界面优化优先级 | ⏸ 延后 | "先暂时延后" |
| 5 | 客户端内功能优先级 | ⏸ 延后 | "先暂时延后" |
| ★ | **本阶段目标** | 🎯 **先成功打包出来** | "我们的目标是先成功打包出来" |

### 7.1 "完全独立"意味着什么（已核实规模）

**独立 = 三样东西都不与别人共用：**

| 项 | 社区版 / 全局 | **本项目** |
|---|---|---|
| 内核 | `AppData\...\resources\app\node_modules` 或 `%APPDATA%\npm\...` | `vendor\dsh\`（自带） |
| 数据目录 | `~/.dsh` | **独立 DSH_HOME**（如 `%APPDATA%\DshIntegrated\`） |
| 端口 | 3080 | **独立端口**（如 3180） |

**已核实的好处**：三样都独立后，**冲突面 1/2/3/4 全部消失**（见 §〇），
两个客户端可以同时开着互不影响。

**已核实的成本**：要重建一份环境。

```
当前 ~/.dsh 规模：635.5 MB
  ├─ .npm-cache-upgrade  237.9 MB  ← ⚠️ 这是上次升级留下的垃圾，可直接删
  ├─ safety              260.4 MB  ← 主要是 dsh-backup 里的备份
  ├─ sessions            122.9 MB  ← 会话记录（数据）
  ├─ profiles              5.7 MB  ← 插件与配置（数据）
  ├─ tools / notes / skills  ~4 MB ← 自有脚本（要搬）
  └─ storages              0.8 MB  ← 投影缓存（数据）
```

**要分清"数据"与"环境"**：

| 类别 | 内容 | 独立后怎么办 |
|---|---|---|
| **环境** | `profiles/node_modules`（241 包镜像） | 重建（npm install 自动生成） |
| **环境** | 内核依赖（190 包） | 重建 |
| **数据** | `sessions/`（会话记录） | ⚠️ **可选延续**（见 7.2） |
| **数据** | `profiles/*/package.json`（插件清单） | ⚠️ **可选延续** |
| **资产** | `tools/*.mjs`、`safety/*.ps1`（你自己的脚本） | ✅ **要搬过来复用** |

### 7.2 ⚠️ 一个必须你决定的点：会话记录要不要延续

独立 DSH_HOME 之后，**默认看不到你现在的历史会话**（因为会话存在 `~/.dsh/sessions/`）。

两个选择：

| 方案 | 做法 | 好处 | 代价 |
|---|---|---|---|
| **干净起步**（推荐先做） | 全新 DSH_HOME，什么都不带 | 最干净，能验证"独立"是否真的成立 | 历史会话要回社区版/浏览器看 |
| **延续数据** | 把 `sessions/` 和 `profiles/*/package.json` 复制过去 | 无缝衔接 | 引入耦合，"独立"不彻底；且 122.9 MB |

> 💡 **建议**：**先干净起步**。理由：
> ① 本阶段目标是"先成功打包出来"，干净环境最容易排查问题；
> ② 数据延续**随时可以补**（复制两个目录即可），不是不可逆决定；
> ③ 真正的"独立验证"必须在干净环境下做 —— 带着旧数据测不出隔离是否生效。

### 7.3 可复用的自有资产（**这些是你写的，不是抄社区版**）

**崩溃自检类**（`~/.dsh/safety/`，共 12 个脚本）：

| 脚本 | 大小 | 复用方式 |
|---|---|---|
| `dsh-check.ps1` | 11.1 KB | 自检逻辑 → 搬进界面 |
| `dsh-boottest.ps1` | 4.3 KB | 试启动逻辑 → 核心复用 |
| `dsh-clean.ps1` | 10.8 KB | 纯净模式 → 逃生通道 |
| `dsh-restore.ps1` | 8.7 KB | 恢复逻辑 |
| `dsh-browser.ps1` | 8.9 KB | 浏览器模式 → 逃生通道 |
| `dsh-rollback.ps1` | 4.5 KB | 回滚逻辑 |
| `dsh-reset.ps1` | 11 KB | 重置逻辑 |
| `dsh-upgrade.ps1` | 11.8 KB | ⚠️ 见下方警告 |
| `dsh-upgrade-rollback.ps1` | 8.6 KB | ⚠️ 见下方警告 |

**模型通道类**（`~/.dsh/tools/`，共 9 个）：

| 脚本 | 大小 | 用途 |
|---|---|---|
| `dsh-model-channel.mjs` | 20.2 KB | 通道增删启停 |
| `workbuddy-roster-refresh.mjs` | 15.6 KB | 名册刷新 |
| `dsh-context-meter.mjs` | 13.2 KB | 上下文水位 |
| `switch-channel-model.mjs` | 11.9 KB | 换模型 |
| `qfmodel-1m-context.mjs` | 6.9 KB | 上下文调整 |
| `patch-qoder-context-clamp.mjs` | 5.2 KB | Qoder 补丁 |
| `qoder-quota-check.mjs` | 2.7 KB | 配额检查 |

> ⚠️ **`dsh-upgrade.ps1` / `dsh-upgrade-rollback.ps1` 的特殊说明**：
> 这两个脚本是 **2026-09-19 事故的当事脚本**，已加装安全锁（默认拒绝运行）。
> **不要直接搬过来当新客户端的更新逻辑** —— 要按 §四 重新设计
> （检查/提示/下载/切换 四步分离，切换必须用户手动）。
> 它们可以作为**反面参考**：看看哪些地方做错了。

### 7.4 与社区版的关系（合规上更简单）

**本项目完全从零写外壳、只用官方内核 ⇒ 不涉及 chyra-moon 的版权。**

| 层 | 版权人 | 本项目的义务 |
|---|---|---|
| `@deepseek-ai/*`（官方内核） | DeepSeek | **MIT，保留版权声明** |
| ~~chyra-moon 外壳~~ | ~~chyra-moon~~ | ✅ **不涉及**（没用到它的代码） |
| 本项目外壳 | 你 | 你自己的署名 |

⇒ 合规比社区版简单：**只需保留 DeepSeek 的 MIT 声明 + 第三方依赖清单**。
（`THIRD-PARTY-LICENSES` 的生成脚本可以从社区版**参考**写法，那是通用做法不是它的代码。）

---

## 八、与社区版（`4.DSH桌面版定制`）的关系

你说：**参考不抄**。我理解成：

| ✅ 可以 | ❌ 不可以 |
|---|---|
| 参考它的**问题清单**（哪些坑踩过） | 复制它的代码当自己的 |
| 参考它的**架构思路** | 沿用它的致命结构（内核嵌入安装目录） |
| 搬**你自己写的**东西（崩溃工具箱逻辑） | 把它的文件直接拷过来 |
| 用它验证过的**事实**（端口、路径、启动参数） | 继承它的 MIT 署名义务（除非确实用了 chyra-moon 的代码） |

> 💡 **一个实际的好处**：如果本项目**完全从零写外壳**、只用官方内核，
> 那么就**不涉及 chyra-moon 的版权**，署名义务只对 `deepseek-ai`（MIT）——
> 合规上更简单。**这是个值得考虑的点。**

---

## 九、本阶段施工顺序（目标：**先成功打包出来**）

```
第 1 步  项目骨架 + 内核发现 + 启动/复用逻辑        ← 先跑通
第 2 步  打包流程（electron-builder，出 NSIS/便携版）← 本阶段目标
第 3 步  版本检查（只读 + 提示，不自动升级）
第 4 步  崩溃自检面板（搬你自己的脚本逻辑）
第 5 步  界面优化 / 客户端内功能（§三，延后定）
```

**第 1、2 步做完就算达成本阶段目标。**

---

*本文档是 v0.1 草案。§七 的决策已固化，§九 的施工顺序待你确认后开工。*

